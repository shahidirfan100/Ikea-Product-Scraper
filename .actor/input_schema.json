{
  "title": "IKEA Product Scraper",
  "description": "API-first IKEA product scraper. Fetches listing/search product data directly from internal API without visiting product detail pages.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "Optional IKEA listing/search URLs. For search pages like https://www.ikea.com/gb/en/search/?q=chair, query is extracted and fetched through internal API.",
      "editor": "requestListSources",
      "default": [],
      "prefill": []
    },
    "country": {
      "title": "Country",
      "type": "string",
      "description": "IKEA country code (e.g., 'gb' for United Kingdom, 'us' for United States, 'de' for Germany)",
      "editor": "select",
      "default": "gb",
      "prefill": "gb",
      "enum": ["gb", "us", "de", "fr", "se", "no", "dk", "fi", "nl", "be", "ch", "at", "it", "es", "pt", "pl", "cz", "sk", "hu", "ro", "hr", "si", "ie", "ca", "au", "jp", "kr", "cn", "tw", "hk", "sg", "my", "th", "ae"],
      "enumTitles": ["United Kingdom", "United States", "Germany", "France", "Sweden", "Norway", "Denmark", "Finland", "Netherlands", "Belgium", "Switzerland", "Austria", "Italy", "Spain", "Portugal", "Poland", "Czech Republic", "Slovakia", "Hungary", "Romania", "Croatia", "Slovenia", "Ireland", "Canada", "Australia", "Japan", "South Korea", "China", "Taiwan", "Hong Kong", "Singapore", "Malaysia", "Thailand", "United Arab Emirates"]
    },
    "language": {
      "title": "Language",
      "type": "string",
      "description": "Language code for the website (e.g., 'en' for English, 'de' for German)",
      "editor": "textfield",
      "default": "en",
      "prefill": "en"
    },
    "category": {
      "title": "Category",
      "type": "string",
      "description": "Category or fallback search term. Supports special new-page categories (e.g., 'new-products') and plain search terms.",
      "editor": "textfield",
      "default": "new-products",
      "prefill": "new-products"
    },
    "maxProducts": {
      "title": "Maximum Products",
      "type": "integer",
      "description": "Maximum number of products to scrape. Set to 0 for unlimited.",
      "editor": "number",
      "default": 20,
      "prefill": 20,
      "minimum": 0
    },
    "maxPages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum API pagination windows to process (safety limit).",
      "editor": "number",
      "default": 2,
      "prefill": 2,
      "minimum": 1
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings to avoid rate limiting and blocking. Residential proxies recommended for best results.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}