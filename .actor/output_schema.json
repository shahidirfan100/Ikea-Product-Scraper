{
    "actorOutputSchemaVersion": 1,
    "title": "IKEA Product Scraper Output",
    "properties": {
        "overview": {
            "type": "string",
            "title": "Product Overview",
            "description": "View API-first listing/search products with key fields",
            "template": "{{links.apiDefaultDatasetUrl}}/items?view=overview"
        },
        "allProducts": {
            "type": "string",
            "title": "All Products (JSON)",
            "description": "Download all product data in JSON format",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}